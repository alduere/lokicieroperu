---
import Base from "../../../layouts/Base.astro";
import { indecopiSortedDates, getIndecopiDay, formatFechaEs } from "../../../lib/data";

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const days = indecopiSortedDates.map((d) => ({ date: d, dia: getIndecopiDay(d)! }));
---

<Base title="Archivo · INDECOPI Alertas · Loki" sourceSlug="indecopi" sourceName="INDECOPI">
  <nav class="text-xs text-txt-dim mb-4">
    <a href={`${base}/`} class="hover:text-txt">Inicio</a>
    <span class="mx-1">&rsaquo;</span>
    <a href={`${base}/indecopi/`} class="hover:text-txt">INDECOPI</a>
    <span class="mx-1">&rsaquo;</span>
    <a href={`${base}/indecopi/alertas/`} class="hover:text-txt">Alertas</a>
    <span class="mx-1">&rsaquo;</span>
    <span class="text-txt">Archivo</span>
  </nav>

  <div class="mb-8">
    <h1 class="font-display text-4xl font-bold tracking-tightest">Archivo</h1>
    <p class="text-txt-dim mt-2">Historial de alertas de consumo de INDECOPI</p>
  </div>

  {days.length > 0 ? (
    <div class="overflow-hidden border-t-2 border-edge">
      <table class="w-full text-sm">
        <thead class="bg-loki-surface border-b-2 border-edge">
          <tr>
            <th class="text-left px-4 py-3 font-sans font-semibold text-txt-dim uppercase text-xs tracking-wider">Fecha</th>
            <th class="text-right px-4 py-3 font-sans font-semibold text-txt-dim uppercase text-xs tracking-wider">Alertas</th>
          </tr>
        </thead>
        <tbody>
          {days.map(({ date, dia }) => (
            <tr class="border-b border-edge hover:bg-loki-surface/50">
              <td class="px-4 py-3">
                <a href={`${base}/indecopi/alertas/`} class="font-display font-semibold text-txt hover:text-txt-muted">
                  {formatFechaEs(date)}
                </a>
              </td>
              <td class="px-4 py-3 text-right font-mono text-txt-muted">{dia.stats.total_alertas}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  ) : (
    <div class="border border-edge rounded-lg p-12 text-center">
      <p class="text-txt-muted">Aun no hay datos de alertas en el archivo.</p>
    </div>
  )}
</Base>

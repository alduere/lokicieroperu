---
import type { NormaResumida } from "../lib/types";

interface Props {
  normas: NormaResumida[];
}

const { normas } = Astro.props;

// Compute counts per tipo and per impacto
const tipoCounts = new Map<string, number>();
const impactoCounts = new Map<string, number>();
const sectorCounts = new Map<string, number>();
for (const n of normas) {
  tipoCounts.set(n.tipo_corto, (tipoCounts.get(n.tipo_corto) || 0) + 1);
  impactoCounts.set(n.impacto, (impactoCounts.get(n.impacto) || 0) + 1);
  for (const s of n.sectores) sectorCounts.set(s, (sectorCounts.get(s) || 0) + 1);
}
const tipos = [...tipoCounts.entries()].sort((a, b) => b[1] - a[1]);
const sectores = [...sectorCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 12);

const TIPO_LABELS: Record<string, string> = {
  DS: "Decreto Supremo",
  DU: "Decreto de Urgencia",
  DL: "Decreto Legislativo",
  LEY: "Ley",
  RM: "R. Ministerial",
  RVM: "R. Viceministerial",
  RD: "R. Directoral",
  RS: "R. Superintendencia",
  RJ: "R. Jefatural",
  RA: "R. Administrativa",
  RPRE: "R. Presidencia",
  RCD: "R. Consejo Directivo",
  RSU: "R. Suprema",
  RES: "Resolución",
  OR: "Ordenanza Regional",
  OM: "Ordenanza Municipal",
  ORD: "Ordenanza",
  AC: "Acuerdo de Consejo",
  ACU: "Acuerdo",
  CIRC: "Circular",
  DIR: "Directiva",
  FE: "Fe de Erratas",
  REG: "Reglamento",
  PROT: "Protocolo",
  OTRO: "Otro",
};
---

<div class="glass p-4 sm:p-5 mb-6" id="filters">
  <div class="flex items-center gap-2 flex-wrap">
    <span class="text-xs font-semibold text-ink-500 uppercase tracking-widest mr-2">Filtrar:</span>

    <div class="flex flex-wrap gap-1.5">
      <button
        class="filter-chip filter-impacto active text-xs px-3 py-1.5 rounded-md bg-ink-900 text-white font-semibold"
        data-filter-type="impacto"
        data-filter-value="all"
      >Todos</button>
      <button
        class="filter-chip filter-impacto text-xs px-3 py-1.5 rounded-md bg-impact-alto/10 text-impact-alto font-semibold hover:bg-impact-alto hover:text-white transition-colors"
        data-filter-type="impacto"
        data-filter-value="alto"
      >🔴 Alto ({impactoCounts.get("alto") || 0})</button>
      <button
        class="filter-chip filter-impacto text-xs px-3 py-1.5 rounded-md bg-impact-medio/10 text-impact-medio font-semibold hover:bg-impact-medio hover:text-white transition-colors"
        data-filter-type="impacto"
        data-filter-value="medio"
      >🟡 Medio ({impactoCounts.get("medio") || 0})</button>
      <button
        class="filter-chip filter-impacto text-xs px-3 py-1.5 rounded-md bg-impact-bajo/10 text-impact-bajo font-semibold hover:bg-impact-bajo hover:text-white transition-colors"
        data-filter-type="impacto"
        data-filter-value="bajo"
      >🟢 Bajo ({impactoCounts.get("bajo") || 0})</button>
    </div>
  </div>

  <details class="mt-3 text-xs">
    <summary class="cursor-pointer text-ink-500 font-semibold uppercase tracking-widest hover:text-ink-900">
      Filtrar por tipo de norma ({tipos.length})
    </summary>
    <div class="mt-2 flex flex-wrap gap-1.5">
      <button class="filter-chip filter-tipo active text-xs px-2.5 py-1 rounded-md bg-ink-900 text-white font-semibold" data-filter-type="tipo" data-filter-value="all">Todos</button>
      {tipos.map(([code, count]) => (
        <button
          class="filter-chip filter-tipo text-xs px-2.5 py-1 rounded-md bg-ink-100 text-ink-700 hover:bg-ink-900 hover:text-white transition-colors font-mono"
          data-filter-type="tipo"
          data-filter-value={code}
          title={TIPO_LABELS[code] || code}
        >{code} ({count})</button>
      ))}
    </div>
  </details>

  {sectores.length > 0 && (
    <details class="mt-3 text-xs">
      <summary class="cursor-pointer text-ink-500 font-semibold uppercase tracking-widest hover:text-ink-900">
        Filtrar por sector ({sectores.length})
      </summary>
      <div class="mt-2 flex flex-wrap gap-1.5">
        <button class="filter-chip filter-sector active text-xs px-2.5 py-1 rounded-md bg-ink-900 text-white font-semibold" data-filter-type="sector" data-filter-value="all">Todos</button>
        {sectores.map(([sector, count]) => (
          <button
            class="filter-chip filter-sector text-xs px-2.5 py-1 rounded-md bg-ink-100 text-ink-700 hover:bg-ink-900 hover:text-white transition-colors"
            data-filter-type="sector"
            data-filter-value={sector}
          >{sector.replace(/_/g, " ")} ({count})</button>
        ))}
      </div>
    </details>
  )}
</div>

<script is:inline>
  (function () {
    const state = { impacto: "all", tipo: "all", sector: "all" };

    function applyFilters() {
      const cards = document.querySelectorAll("[data-norma]");
      let hidden = 0;
      cards.forEach((el) => {
        const tipo = el.getAttribute("data-tipo") || "";
        const impacto = el.getAttribute("data-impacto") || "";
        const sectores = (el.getAttribute("data-sectores") || "").split(",");
        const matchImpacto = state.impacto === "all" || impacto === state.impacto;
        const matchTipo = state.tipo === "all" || tipo === state.tipo;
        const matchSector = state.sector === "all" || sectores.includes(state.sector);
        const visible = matchImpacto && matchTipo && matchSector;
        el.style.display = visible ? "" : "none";
        if (!visible) hidden++;
      });

      // Hide sector cards that have no visible norms left
      document.querySelectorAll("[data-sector-card]").forEach((card) => {
        const visible = card.querySelectorAll('[data-norma]:not([style*="display: none"])');
        card.style.display = visible.length > 0 ? "" : "none";
      });

      const counter = document.getElementById("filter-count");
      if (counter) {
        const total = cards.length;
        counter.textContent = hidden > 0 ? `Mostrando ${total - hidden} de ${total}` : `${total} normas`;
      }
    }

    document.querySelectorAll(".filter-chip").forEach((btn) => {
      btn.addEventListener("click", () => {
        const type = btn.getAttribute("data-filter-type");
        const value = btn.getAttribute("data-filter-value");
        if (!type || !value) return;
        state[type] = value;
        document.querySelectorAll(`.filter-${type}`).forEach((b) => {
          b.classList.toggle("active", b.getAttribute("data-filter-value") === value);
          if (b.classList.contains("active")) {
            b.classList.add("bg-ink-900", "text-white");
            b.classList.remove("bg-ink-100", "text-ink-700");
          } else if (type === "impacto") {
            // restore impacto colors
            const v = b.getAttribute("data-filter-value");
            b.classList.remove("bg-ink-900", "text-white");
            if (v === "alto") b.classList.add("bg-impact-alto/10", "text-impact-alto");
            else if (v === "medio") b.classList.add("bg-impact-medio/10", "text-impact-medio");
            else if (v === "bajo") b.classList.add("bg-impact-bajo/10", "text-impact-bajo");
          } else {
            b.classList.remove("bg-ink-900", "text-white");
            b.classList.add("bg-ink-100", "text-ink-700");
          }
        });
        applyFilters();
      });
    });
  })();
</script>
